services:
  qa-platform:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qa-platform-frontend
    ports:
      - "5175:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

networks:
  default:
    name: qa-platform-network
